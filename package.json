{
  "name": "save-the-world-game",
  "version": "1.0.0",
  "description": "AI Strategy Game",
  "scripts": {
    "test": "node -e \"require('./tests.js').runAllTests().then(result => process.exit((typeof result === 'object' ? result.passed : result) ? 0 : 1))\"",
    "lint": "eslint *.js",
    "lint:unused": "eslint --rule 'no-unused-vars: error' *.js"
  },
  "devDependencies": {
    "eslint": "^9.30.0"
  }
}